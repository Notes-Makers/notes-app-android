mutation UpdateNote(
    $title: String!,
    $description: String!,
    $noteId: ID!
) {
    updateNote(
        body: {
            title: $title,
            description: $description,
            noteId: $noteId
        }
    )
}