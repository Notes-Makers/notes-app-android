query GetNote($noteId: ID!) {
    getNote(noteId: $noteId) {
        id
        name
        description
        createdAt
        createdBy
        modifiedAt
        modifiedBy
        isPrivate
        isShared
        isDeleted
        tag
    }
}